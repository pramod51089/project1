variable "container_definitions" {
  description = "Map of container definitions for ECS Task Definition"
  type        = map(object({
    name  = string
    image = string
    # Add other container attributes as needed
  }))
  default     = {}
}
